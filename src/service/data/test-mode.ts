export const sKTst = {
  c: 'sKTst',
  e: 0
  // "d": () => {
  //   let i = 0;
  //   const id = setInterval(() => {
  //     const btnNames = ['UP', 'DOWN', 'LEFT', 'RIGHT', 'A', 'B', 'X', 'Y'];
  //     const genKs: string[] = [];
  //     Array.from({ length: Math.floor(Math.random() * 5) }).forEach(() => {
  //       let name = btnNames[Math.floor(Math.random() * btnNames.length)];
  //       while (genKs.includes(name)) {
  //         name = btnNames[Math.floor(Math.random() * btnNames.length)];
  //       }
  //       genKs.push(name);
  //     });
  //     const ks = {};
  //     genKs.forEach(name => {
  //       ks[name] = {
  //         k: name,
  //         pr: Math.random() > 0.5 ? 1 : 0
  //       };
  //     });
  //     i++;
  //     if (i > 500) {
  //       clearInterval(id);
  //     }
  //   }, 500);
  // }
};
